# LaTeX Report

Main source file:

```bash
main.tex
```

Compile from this folder with any standard LaTeX distribution:

```bash
pdflatex main.tex
pdflatex main.tex
```

The second run updates the table of contents. The report uses standard packages only and does not require `minted` or shell escape. Figures are stored in `figures/` with compiler-safe filenames.
